spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/manga_novel_test
    username: postgres
    password: postgres
  flyway:
    enabled: true

artverse:
  api-token: ""
  deepseek:
    api-key: "test-key"
  image:
    api-key: "test-key"
  minio:
    access-key: "test-key"
    secret-key: "test-secret"
